1)npx create-react-app client - will create a react application in a folder named client
2)npm run dev is used to run frontend and backend cocurrently
"server": "nodemon server",
"client": "npm start --prefix client",
"dev": "concurrently \"npm run server\" \"npm run client\""
Now npm run dev will start frontend from client folder and server at the same time 
3)Fragment is just like a ghost element i.e it will never show in the DOM
4)exact path = '/' component = {Landing} is used to reach the Landing component
5)Ctrl + D to select all the elements which we want to change
6)[e.target.name] will allow us to update all the fields with various names and we won't have to hard code it every time